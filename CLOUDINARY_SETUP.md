# 📸 Настройка хранения изображений

## Проблема
Изображения, загруженные в `static/uploads/`, удаляются при каждом новом деплое на Render, так как это эфемерное хранилище.

---

## 🎯 Доступные решения

### **Вариант 1: PostgreSQL база данных (РЕКОМЕНДУЕТСЯ)** 🗄️
✅ **Бесплатно** - использует существующую базу данных  
✅ **Постоянное хранение** - изображения не исчезают  
✅ **Без регистрации** - работает сразу  
✅ **Простая настройка** - одна переменная окружения  

**Настройка:**
```env
IMAGE_STORAGE=database
```
Готово! Изображения автоматически сохраняются в PostgreSQL.

### **Вариант 2: Cloudinary** ☁️
✅ CDN для быстрой доставки по всему миру  
✅ 25 ГБ бесплатно  
✅ Автоматическая оптимизация  

**Настройка:**
1. Зарегистрируйтесь на https://cloudinary.com/users/register_free
2. Получите учетные данные в Dashboard
3. Добавьте в Environment Variables на Render:
```env
CLOUDINARY_CLOUD_NAME=ваш_cloud_name
CLOUDINARY_API_KEY=ваш_api_key
CLOUDINARY_API_SECRET=ваш_api_secret
IMAGE_STORAGE=cloudinary
```

### **Вариант 3: Локальное хранилище** 📁
⚠️ **Не рекомендуется** - изображения удаляются при деплое  
Только для локальной разработки:
```env
IMAGE_STORAGE=local
```

---

## 🚀 Быстрый старт (PostgreSQL)

### Шаг 1: Обновите переменные окружения на Render

1. Откройте ваш веб-сервис на Render
2. Перейдите в **Environment** → **Environment Variables**
3. Добавьте или обновите переменную:
   ```
   IMAGE_STORAGE=database
   ```
4. Нажмите **Save Changes** (сервис автоматически перезапустится)

### Шаг 2: Проверьте работу

1. Перейдите в админ-панель
2. Загрузите изображение для товара
3. Изображение сохранится в PostgreSQL и не исчезнет!

---

## 📊 Сравнение вариантов

| Критерий | PostgreSQL | Cloudinary | Local |
|----------|-----------|------------|-------|
| **Стоимость** | Бесплатно | 25GB бесплатно | Бесплатно |
| **Постоянство** | ✅ Да | ✅ Да | ❌ Нет |
| **CDN** | ❌ Нет | ✅ Да | ❌ Нет |
| **Настройка** | 1 минута | 5 минут | 0 минут |
| **Регистрация** | Не нужна | Нужна | Не нужна |

---

## 🔧 Технические детали (PostgreSQL)

### Как это работает:
1. Изображение загружается через форму
2. Конвертируется в бинарный формат
3. Сохраняется в таблице `images` с полями:
   - `filename` - уникальное имя
   - `data` - бинарные данные (LargeBinary)
   - `mime_type` - тип файла (image/jpeg, image/png)
   - `size` - размер в байтах
4. При запросе изображение отдается через `/images/<filename>`

### Ограничения:
- Максимальный размер: **16 МБ** на файл
- Поддерживаемые форматы: PNG, JPG, JPEG, GIF, WebP
- База данных Render: до **1 ГБ** на бесплатном плане

### Оптимизация:
Для большого каталога рекомендуется:
- Сжимать изображения перед загрузкой
- Использовать WebP формат (меньше размер)
- Или перейти на Cloudinary для CDN

---

## 🆘 Решение проблем

**Проблема:** Изображения не отображаются  
**Решение:** Проверьте, что `IMAGE_STORAGE=database` в Environment Variables

**Проблема:** Ошибка "Файл слишком большой"  
**Решение:** Сожмите изображение до < 16 МБ

**Проблема:** База данных переполнена  
**Решение:** Перейдите на Cloudinary или платный план Render

---

## 📈 Миграция существующих изображений

Если у вас уже есть изображения в `static/uploads/`:
1. Они останутся работать локально
2. Новые изображения будут сохраняться в БД
3. После деплоя старые изображения исчезнут (были эфемерные)
4. Загрузите их заново через админ-панель
